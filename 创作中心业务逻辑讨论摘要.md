# AI 电商图片 - 创作中心业务逻辑讨论摘要

> 用途：记录 2026-07-19 关于创作广场、自由生图、参考图复刻、套图创作、任务记录、精品库与模板中心的讨论结论，供后续原型设计和主线任务承接使用。

## 1. 总体信息架构

建议把“创作发起、任务过程、成品资产、创作方法”拆成四个明确层次：

```text
创作广场：发起创作、选择创作方式与资源
创作记录：查看任务过程、进度、失败原因与重试
精品库：沉淀用户确认后的生成图片和套图成品
模板中心：沉淀可复用的创作结构、提示词规则与参数配置
```

原则：**任务、资产、模板不是同一类数据，不能互相替代。**

| 对象 | 它是什么 | 用户主要目的 |
| --- | --- | --- |
| 创作任务 | 一次异步执行过程 | 查看进度、继续、重试、追溯 |
| 精品资产 | 已确认的具体生成结果 | 查找、使用、下载、作为素材复用 |
| 创作模板 | 可复用的生成方法/配方 | 换商品后按同样方法再次创作 |

## 2. 创作广场：一个入口，多个明确模式

左侧导航可保留一个“创作广场”入口，避免菜单过碎；但广场内不应使用同一个输入框，通过是否上传图片来隐式判断模式。

建议在创作广场明确展示三个入口：

1. **自由生图**
2. **参考图复刻**
3. **套图创作**

三者可共用平台壳、上传控件、参数栏与生成机制，但进入各自独立的配置/详情工作流。

### 2.1 自由生图与参考图复刻为何不能完全合并

两者的提示词逻辑不同：

| 模式 | 提示词表达 | 素材规则 | 生成重点 |
| --- | --- | --- | --- |
| 自由生图 | 描述“希望生成什么画面” | 商品图可选或推荐 | 文案与商品共同决定新画面 |
| 参考图复刻 | 描述“相对参考图只需要改什么” | 商品图、参考图通常必填 | 参考图决定构图、光影、场景等；商品需保持准确 |

参考图复刻的提示词应更像“差异补充”，而非让用户重复描述整张图。例如提示“保留构图，仅替换背景为浅色卧室；保持商品主体不变”。因此，二者应该是**显式模式切换**，而不是同一页面的隐式分支。

## 3. 套图创作：入口合并，工作流独立

### 3.1 入口

在创作广场增加明确的“套图创作”按钮/卡片。点击后进入独立的套图创作详情，而非沿用自由生图的大输入框。

建议首屏包含：

- 新建套图创作；
- 从模板开始创作（主入口）；
- 我的套图/最近任务（用于继续、复用，不应是唯一的新建入口）。

用户侧统一使用“套图创作”名称；“模块”用于工作流内部，例如主图模块、卖点模块、详情模块。

### 3.2 工作流

```text
创作广场点击“套图创作”
→ 选择模板 / 新建套图
→ 配置商品、参考图、模特及各模块内容
→ 确认生成
→ 套图任务详情（模块级进度与选图）
→ 确认保存成品到精品库 / 按需保存为模板
```

套图不是“多生成几张图”，而是按预设内容结构完成一组商品图。因此需要支持：

- 主图、卖点图、详情图、SKU 图等模块；
- 模块级状态与进度；
- 单模块选图；
- 仅补生成失败模块；
- 整套保存或仅保存选中结果。

### 3.3 点击“生成”后的去向

不建议直接回到创作广场或创作记录。

应跳转至 **套图任务详情页**，用于展示：

- 排队中、生成中、部分完成、已完成、失败等状态；
- 每个模块的生成进度和候选图；
- 失败原因、处理建议、仅重试失败模块；
- 完成后的选图、保存、下载、再创作操作。

用户离开详情页后，任务仍在后台运行；后续可从“创作记录”回到该详情页。

## 4. 创作记录：仅承接过程，不承担资产库职责

创作记录更适合文字列表/任务列表形态，承接：

- 草稿；
- 排队中、生成中；
- 部分完成；
- 已完成；
- 失败、取消；
- 进度、失败阶段、重试和继续创作。

建议“进行中”作为汇总筛选口径，包含“排队中”和“生成中”。

按任务类型分流详情：

| 任务类型 | 点击“查看详情”后的去向 |
| --- | --- |
| 单图生成 / 参考图复刻 | 图片生成任务详情 |
| 套图创作 | 套图任务详情 / 多模块选图流程 |
| 扩图 | 扩图编辑器与结果详情 |

历史缩略图可以作为“我的套图/最近任务”的继续入口：

- 草稿、生成中、失败：继续创作、重试失败模块；
- 已完成：查看结果、基于此再创作；
- 不应把已完成历史任务当成唯一的新建入口，也不应覆盖历史任务。

## 5. 精品库：统一承接确认后的生成资产

现有“精品库”定位为最终的用户成品资产库。自由生图、参考图复刻、模块生图、套图批量复刻、基于模板批量生图的结果，均可点击“保存到精品库”，进入对应的品类和资产类型。

这意味着不需要再单独创建“我的套图”资产库；套图资产应落在精品库中。

建议精品库的结构：

```text
精品库
└─ 商品类目（如：内衣）
   ├─ 单图
   ├─ 模块图
   └─ 套图
      └─ 无痕内衣夏季详情套图（9 张）
```

套图在精品库中应以“整套资产卡片”展示：封面图 + 图片数量 + 商品类目/标签；进入卡片后浏览和操作套内图片。不要把套图的所有结果拆成无关联的散图平铺。

### 5.1 精品库保存规则

- 任务完成不等于自动成为精品资产；
- 用户完成选图后，点击“保存到精品库”才沉淀为正式资产；
- 套图支持“保存整套到精品库”或“保存选中图片到精品库”；
- 未筛选候选图、失败图、测试图仍保留在任务详情/创作记录中，不污染精品库；
- 精品库资产可用于下载、查看、作为参考图、再次创作，以及进入后续素材选择流程。

## 6. 模板中心：只沉淀可复用的方法

模板中心不应被当作成品图片库。其核心内容是可复用的“创作配方”：

- 套图模块结构；
- 各模块的提示词规则；
- 通用参数（比例、模型、数量等）；
- 商品图、参考图、模特图等素材槽位；
- 可选的示例图和适用商品类目。

模板中心建议至少包含：

- 官方模板；
- 我的模板。

### 6.1 “保存到精品库”与“保存为模板”的区别

| 操作 | 保存内容 | 下次的使用方式 |
| --- | --- | --- |
| 保存到精品库 | 已生成且选中的具体成品图/模块图/套图 | 当作图片资产、参考素材或可交付成品使用 |
| 保存为模板 | 模块结构、可泛化提示词、参数、素材槽位 | 换一款商品后按同一方法再次生成 |

示例：

- 精品库中保存的是“无痕内衣详情套图”的具体九张成品；
- 模板中心中保存的是“无痕内衣详情套图”的生成配方：主图/卖点图/细节图结构、对应提示词和参数。之后可以换另一款内衣继续使用。

### 6.2 保存为模板的产品规则

“保存为模板”属于进阶动作，不能和“保存到精品库”混为同一主操作。

建议：

- “保存到精品库”作为生成结果的主操作；
- “保存为模板”作为次级操作或“更多操作”；
- 保存模板前进入整理/确认页，要求用户确认：
  - 保留哪些模块；
  - 将具体商品名称改为可复用描述；
  - 商品图、参考图、模特图各自的素材槽位；
  - 默认参数；
  - 是否使用当前成品作示例图；
  - 仅保存为我的模板，或提交上架（如未来支持）。

## 7. 套图任务完成后的建议操作优先级

1. `保存整套到精品库` / `保存选中图片到精品库`（主操作）
2. `保存为模板`（次级、需确认泛化规则）
3. `下载全部`
4. `继续编辑`
5. `补生成失败模块`

## 8. 后续设计待确认项

1. 自由生图与参考图复刻是采用顶部模式切换、入口卡片，还是分别进入独立详情页；结论应是“显式切换”，不能靠上传素材隐式判断。
2. 套图创作是否允许“从空白新建”；若允许，应提供最小模块结构，模板创作仍为优先入口。
3. 精品库中保存套图时，是否允许同时保存“整套”和“单张”；建议两者都支持，但优先保持整套资产关系。
4. 模板是否支持公开/上架；如支持，需要增加审核、版本和引用关系规则。
5. 生成扣费、失败退款、重试扣费规则需由产品统一定义，并在提交生成前明确展示。

