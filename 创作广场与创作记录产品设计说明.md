# AI 电商图片 - 创作广场与创作记录产品设计说明

> 版本：v1.0  
> 更新日期：2026-07-20  
> 适用原型：`design-output/ai-ecommerce-product-library-v3`

## 1. 设计目标

在不改变“创作广场先直接创作”的页面结构下，明确自由创作与参考图复刻的业务边界，并为创建模块、套图模板创作、创建 AI 模特和智能扩图提供快捷入口。

所有图片相关任务统一沉淀到“创作记录”；用户确认有价值的结果后，再主动加入“精品库”，作为后续参考、下载或再次创作的资产来源。

## 2. 信息架构与边界

```text
创作广场：从新想法、新图片或新素材发起创作
创作记录：查看任务过程、结果、状态、失败原因和再次处理
精品库：管理已确认的成品图片、套图与可复用的图片资产
模板中心：管理可复用的套图结构、模块配置与参数规则
模特库：管理可复用的 AI 模特资产
```

原则：任务不是资产。任务完成后不会自动进入精品库；用户选定结果并点击“加入精品库”后，才形成可复用的精品资产。

## 3. 创作广场

### 3.1 主创作框：显式模式切换

创作广场首屏保留大文本输入框。输入框上方设置两个 Tab：

| Tab | 适用场景 | 文本输入含义 | 素材要求 |
| --- | --- | --- | --- |
| 自由创作 | 从商品卖点、场景或创意生成新图 | 描述希望生成的画面 | 商品图、参考图、模特均可选 |
| 参考图复刻 | 复用已有图片的构图、光影、姿势或场景 | 只填写相对参考图需要调整的内容 | 商品图和主参考图必填；模特可选 |

不允许再根据“是否上传参考图”自动判断模式。用户上传参考图但仍在自由创作模式时，该图仅作为灵感或风格辅助；只有在参考图复刻 Tab 下才进入复刻提示词链路。

### 3.2 快捷创作入口

大文本框下方增加“进阶创作”横向快捷入口：

| 快捷入口 | 说明 | 目标页面/流程 |
| --- | --- | --- |
| 从图片创建模块 | 从好图反推可复用规则并测试生成 | 图片解析 → 固定项/变量项确认 → 测试 → 保存模块 |
| 套图模板创作 | 选择套图模板，为一个商品生成主图和详情图 | 选择模板 → 补齐素材 → 多模块生成与选图 |
| 创建 AI 模特 | 生成可复用的电商模特资产 | 模特生成 → 确认 → 保存至模特库 |
| 智能扩图 | 补全已有图的画面或转换比例 | 选择图片 → 扩图编辑器 → 生成结果 |

“基于模块生图”和“基于模板批量生图”不作为创作广场的重复快捷入口：

- 基于模块生图：从精品库/模块资产中选择模块，点击“去创作”后进入创作详情。
- 基于模板批量生图：从模板中心或精品库中的模板资产点击“去创作”，进入同一个套图创作详情。

这样，创作广场负责从零发起；资产库负责从成熟资产复用，不会出现同一条生成链路的两个并列入口。

## 4. 模块、模板与精品资产

```text
好图 / 历史结果
→ 从图片创建模块
→ 模块资产
→ 多个模块编排
→ 套图模板
→ 套图创作
→ 确认结果
→ 精品资产
```

- 模块：单张或局部画面的可复用规则，包含反推提示词、素材槽位、场景/构图约束和示例效果。
- 模板：多个模块的结构化组合，决定一整套主图、卖点图、详情图的顺序和固定内容。
- 精品资产：用户确认后的具体图片或套图结果，可作为参考素材、下载成品或再次创作来源。

创建模块的产物应保存为模块资产；创建 AI 模特的产物应保存为模特资产。两类任务会进入创作记录，但不显示“加入精品库”，以免将规则/人物资产与最终商品图片混淆。

## 5. 创作记录统一规则

### 5.1 记录范围

创作记录统一承接以下异步任务：

- 自由创作；
- 参考图复刻；
- 套图模板创作；
- 智能扩图；
- 从图片创建模块；
- 创建 AI 模特；
- 由精品库或模板中心发起的模块/模板复用任务。

每条记录至少包含：任务类型、状态、标题、关联商品/素材、参数摘要、创建时间、进度或失败原因、查看详情入口。

### 5.2 状态

```text
草稿 → 排队中 → 生成中 → 已完成
                     ├─ 部分完成
                     └─ 失败 / 已取消
```

“进行中”汇总口径包含排队中和生成中；套图任务支持模块级完成度和“仅补生成失败模块”。

### 5.3 加入精品库

图片、套图和扩图任务完成后，在任务详情或创作记录卡片提供主操作：`加入精品库`。

| 结果类型 | 加入规则 | 记录状态标签 |
| --- | --- | --- |
| 单图 | 可选择 1 张或多张候选图加入 | 已加入精品库 / 已加入 2 张 |
| 套图 | 可保存整套或选中图片 | 整套已加入精品库 / 已加入 6/9 张 |
| 扩图 | 将最终扩图结果加入 | 已加入精品库 |

点击后，任务卡更新为“已加入精品库”标签，并将主按钮改为“前往精品库”。标签仅表示该任务已有结果入库，不表示所有候选图均已入库；部分入库时必须展示数量。

模块创建和模特创建改用对应资产状态标签：`已保存至模块库`、`已保存至模特库`。

## 6. 二次创作

精品库中的图片、套图和模板均支持二次创作，但入口按资产类型分流：

| 精品资产 | 二次创作动作 |
| --- | --- |
| 单张图片 | 作为自由创作参考图、参考图复刻主参考图、扩图源图 |
| 套图成品 | 查看套内图片、选择单张再创作，或保存为模板 |
| 模块资产 | 去创作，进入基于模块生图详情 |
| 模板资产 | 去创作，进入套图模板创作详情 |

所有二次创作均创建新的任务记录，保留来源资产 ID 与父任务关系；原任务及原精品资产不被覆盖。

## 7. 原型验收点

- 创作广场的自由创作与参考图复刻可显式切换，文案、必填提示和按钮文案同步变化。
- 下方快捷入口可分别进入模块创建、套图创作、模特创建和扩图流程。
- 单图、套图、扩图、模块创建和模特创建均可在创作记录中追溯。
- 图片/套图/扩图任务可执行“加入精品库”，任务卡显示对应入库状态。
- 精品库资产可作为后续创作的输入；二次创作生成新任务，不覆盖原任务。
